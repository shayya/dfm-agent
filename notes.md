# Agent SDK Notes — Day 2 (Apr 12)

## 1. What is an agent loop?
It runs the whole process of a Claude agent in which the agent calls on certain tools and then returns the output. 

## 2. What is tool use, mechanically?
The agent calls on whichever tools Whether it's terminal or reading or writing a file. that it needs to complete the task and then returns the output. 

## 3. Three open questions
1. Why is it a loop? Or why is it called a loop? 
2. 
3.